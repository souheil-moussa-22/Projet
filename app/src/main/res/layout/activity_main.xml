<?xml version="1.0" encoding="utf-8"?>
<!--
    Page d'accueil : deux boutons de navigation (TP2 - Intents explicites)
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#F0F4FF">

    <!-- Icône / Titre de l'application -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🚗"
        android:textSize="64sp"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CovoiturageApp"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#1565C0"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Proposez ou réservez un trajet"
        android:textSize="16sp"
        android:textColor="#555555"
        android:layout_marginBottom="56dp" />

    <!-- Bouton Ajouter un trajet -->
    <Button
        android:id="@+id/btnAddTrip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="➕  Ajouter un trajet"
        android:textSize="16sp"
        android:layout_marginBottom="16dp" />

    <!-- Bouton Voir les trajets -->
    <Button
        android:id="@+id/btnListTrip"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📋  Voir les trajets"
        android:textSize="16sp" />

</LinearLayout>