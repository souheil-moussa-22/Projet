<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projet">

        <activity android:name=".activities.MyBookingsActivity" android:exported="false" />
        <activity android:name=".activities.BookingActivity" android:exported="false" />
        <activity android:name=".activities.TripDetailsActivity" android:exported="false" />
        <activity android:name=".activities.ListTripActivity" android:exported="false" />
        <activity android:name=".activities.AddTripActivity" android:exported="false" />
        <activity android:name=".activities.MainActivity" android:exported="false" />
        <activity android:name=".activities.SplashActivity" android:exported="false" />
        <activity android:name=".activities.RegisterActivity" android:exported="false" />
        <!-- LoginActivity = seul point d'entrée -->
        <activity android:name=".activities.LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>